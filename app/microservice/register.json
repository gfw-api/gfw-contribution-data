{
    "id": "#(service.id)",
    "name": "#(service.name)",
    "tags": ["gfw"],
    "urls": [{
        "url": "/v1/contribution-data",
        "method": "POST",
        "endpoints": [{
            "method": "POST",
            "path": "/api/v1/contribution-data",
            "data": ["loggedUser"]
        }]
    }]
}
